# correct color display
set -g default-terminal "screen-256color"

# set prefix key to match screen's
set -g prefix C-a
unbind-key C-b

bind-key a send-prefix

# hide status bar
set -g status off

# make sure neovim isn't retarded in tmux
set -sg escape-time 0
